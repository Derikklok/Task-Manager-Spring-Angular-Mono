// This file is generated by Docker/nginx at runtime
// It reads environment variables and makes them available to the Angular app

(function (window) {
  window.__env = window.__env || {};

  // Set API base URL from environment variable or default
  window.__env.apiBaseUrl = '${VITE_API_BASE_URL}' || 'http://localhost:8080/api';
}(window));

